# -*- ruby -*-

APP       = "pointer"
SRCS      = %W{ pointer.cpp }
CXX       = "xcrun"
CXXFLAGS  = ":Os"
LIBS      = ""

DIR = File.dirname __FILE__

load '~/code/build/app2.rake'

task :default => [:osx]
